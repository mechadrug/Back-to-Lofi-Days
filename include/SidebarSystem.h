// #ifndef SIDEBARSYSTEM_H
// #define SIDEBARSYSTEM_H

// #include <SFML/Graphics.hpp>

// class SidebarSystem {
// public:
//     virtual void render(sf::RenderWindow& window) = 0;  // 每个系统的渲染函数
//     virtual void handleClick(const sf::Vector2i& mousePos) = 0;  // 每个系统的点击处理
//     virtual void update() = 0;  // 每个系统的更新函数
//     virtual void open() = 0;    // 打开系统
//     virtual void close() = 0;   // 关闭系统
//     virtual bool isOpen() = 0;  // 系统是否打开
// };

// #endif
